# Task 0.10: Configure Lint/Format Standards

**Date**: 2026-03-10
**Status**: DONE
**Branch**: epic0-claude

## What was done

### New files created
- `.editorconfig` -- Cross-editor formatting baseline (UTF-8, LF, tabs for JS/Svelte/CSS, 4-space indent for Rust, 2-space for YAML, preserve trailing whitespace in Markdown)
- `rustfmt.toml` -- Rust formatting: edition 2021, max_width 100, field_init_shorthand and try_shorthand enabled
- `clippy.toml` -- Clippy configuration: MSRV set to 1.77.2 (matches Cargo.toml rust-version)

### Modified files
- `package.json` -- Added npm scripts for Rust linting/formatting (`lint:rs`, `lint:all`, `format:rs`, `format:rs:check`, `format:all`, `format:all:check`)
- `eslint.config.js` -- Added `src-tauri/target/` to ignores (ESLint was picking up generated JS from Tauri build artifacts)
- `src/app.css` -- Prettier auto-fixed quote style (double -> single) to match `.prettierrc` `singleQuote: true`
- `docs/tickets/epic-0-repository-genesis-infrastructure.md` -- Marked Task [0.7] as DONE

## Issues encountered
- ESLint was reporting errors on `src-tauri/target/debug/build/*/out/__global-api-script.js` (Tauri-generated files). Fixed by adding `src-tauri/target/` to the ignores array in `eslint.config.js`.
- `src/app.css` had double-quoted strings that didn't match the `singleQuote: true` Prettier rule. Auto-fixed via `npx prettier --write`.

## Verification results
All four checks pass:
- `npx eslint .` -- exit 0
- `npx prettier . --check` -- exit 0
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all --check` -- exit 0
- `cargo clippy --manifest-path src-tauri/Cargo.toml --workspace --all-targets -- -D warnings` -- exit 0

## Available npm scripts (full lint/format surface)
| Script | Description |
|---|---|
| `npm run lint` | ESLint (frontend) |
| `npm run lint:rs` | Clippy with `-D warnings` (Rust) |
| `npm run lint:all` | ESLint + Clippy |
| `npm run format` | Prettier write (frontend) |
| `npm run format:check` | Prettier check (frontend) |
| `npm run format:rs` | rustfmt write (Rust) |
| `npm run format:rs:check` | rustfmt check (Rust) |
| `npm run format:all` | Prettier + rustfmt write |
| `npm run format:all:check` | Prettier + rustfmt check |
