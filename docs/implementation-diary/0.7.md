# Task 0.7: Materialize Strict Folder Structure

## Date
2026-03-10

## Objective
Verify and complete the repository directory scaffolding so that every module home
defined in `docs/architecture/01-repository-architecture.md` exists on disk with
appropriate placeholder files for both the Svelte frontend and the Rust backend.

## What Already Existed
- All `src/lib/` directories from the canonical architecture: components/{chrome,layout},
  features/{editor,explorer,git,graph,links,omnibar,plugins,preview,status,theme,workspace},
  ipc/, stores/, styles/, types/, utils/
- All `src-tauri/src/` directories: commands/, core/{content,fs,git,graph,index,links,plugins,search},
  events/, models/, state/
- Rust `mod.rs` stubs for every backend module (created in task 0.6)
- Placeholder `README.md` files in most directories
- Route files: +layout.svelte, +layout.ts, +page.svelte
- Working Cargo.toml, tauri.conf.json, build.rs

## What Was Missing

### Frontend (src/)
- `src/lib/components/editor/` directory (from ARCHITECTURE.md component map)
- `src/lib/components/sidebar/` directory (from ARCHITECTURE.md component map)
- `src/lib/components/omnibar/` directory (from ARCHITECTURE.md component map)
- Barrel `index.ts` files for all `src/lib/` module directories (needed for clean
  imports via `$lib/components`, `$lib/features/editor`, etc.)

### Backend (src-tauri/)
- `src-tauri/src/commands/fs.rs` - file operation command stubs
- `src-tauri/src/commands/git.rs` - git command stubs
- `src-tauri/src/commands/search.rs` - search command stubs
- `src-tauri/src/commands/workspace.rs` - workspace command stubs
- `commands/mod.rs` submodule declarations for the above

## Changes Made
1. Created 3 missing frontend component directories: editor/, sidebar/, omnibar/
2. Created 25 barrel `index.ts` files across all frontend module directories
3. Created 4 Rust command submodule stubs (fs.rs, git.rs, search.rs, workspace.rs)
4. Updated `commands/mod.rs` with `pub mod` declarations for all 4 submodules
5. Verified `cargo check` passes with the expanded module tree

## Architecture Doc Reconciliation
The two architecture documents have minor naming differences:
- `01-repository-architecture.md` uses `src/lib/ipc/` for IPC wrappers
- `vigil-plan/ARCHITECTURE.md` uses `src/lib/tauri/` for the same purpose
- Decision: kept `ipc/` since it already exists and matches the canonical doc

The vigil-plan ARCHITECTURE.md shows `indexer/` and `git/` as top-level src-tauri
modules, but the actual codebase uses `core/index/` and `core/git/` per the canonical
architecture doc. No change needed; the `core/*` pattern is the established convention.

## Verification
- `cargo check` passes cleanly
- `find src -maxdepth 4 -type d | sort` matches canonical architecture
- `find src-tauri -maxdepth 4 -type d | sort` matches canonical architecture
- All 19 Rust source files present under src-tauri/src/
- All 25 TypeScript barrel files present under src/lib/
