
## 2026-03-11T18:03:49-03:00

### 1. Plan
- Unify sidebar render state and shortcut control under `uiStore` (`sidebarOpen` + `sidebarSection`) to remove local-vs-store mismatch.
- Promote `Ctrl+S`, `Ctrl+B`, `Ctrl+N` to global shortcut behavior (same as `Ctrl+P`).
- Implement sidebar auto-hide (3s inactivity), keep existing 200ms sidebar width transition, and reveal via 8px left-edge hover plus `Ctrl+B`.
- Add Explorer notes/collections count presentation using existing mock tree state.
- Ensure `+page.svelte` subscriptions/listeners are explicitly cleaned up.

### 2. Major edits
- `src/routes/+page.svelte`
  - Removed local sidebar source-of-truth (`activeSection`-derived open state) and now renders from `uiStore` state.
  - Updated `PrimaryRail` wiring to reflect/store section through `uiStore` only.
  - Registered `Ctrl+P`, `Ctrl+S`, `Ctrl+B`, `Ctrl+N` all as global shortcuts.
  - Added inactivity timer logic (3000ms), global activity listeners, and left-edge hover reveal strip (`w-2`).
  - Added explicit cleanup for store subscriptions, timers, listeners, and shortcut registrations.
- `src/lib/stores/ui.ts`
  - Added explicit `openSidebar()` and `closeSidebar()` APIs to avoid toggle ambiguity.
- `src/lib/types/store.ts` + `src/lib/stores/settings.ts`
  - Added `autoHideSidebar: boolean` setting with default `true`.
- `src/lib/features/explorer/explorer-store.ts`
  - Added tree summarization helpers and exposed `notesCount` + `collections` summaries.
- `src/lib/features/explorer/ExplorerPanel.svelte`
  - Added notes card and collections list with notes/files counts.

### 3. Validation results
- `npm run check` -> pass (`svelte-check found 0 errors and 0 warnings`).
- `npm run lint` -> pass.
- `npm run build` -> pass.
- `rg -n "ctrl\\+p|ctrl\\+s|ctrl\\+b|ctrl\\+n|register\\(|global" src/routes src/lib/utils` -> confirmed global registrations in `+page.svelte` and shortcut registry behavior.
- `rg -n "autoHideSidebar|sidebarOpen|sidebarSection" src/lib src/routes` -> confirmed unified sidebar state and auto-hide setting usage.
- `git diff --name-only` -> only scoped frontend files modified.

### 4. Risks/handoff notes
- Left-edge hover reveal is implemented as a fixed 8px strip on the viewport edge; this is intentional for deterministic discoverability, but can be adjusted if UX prefers reveal only inside main content bounds.
- Auto-hide relies on mouse/keyboard activity listeners and does not currently distinguish editor focus contexts (by design for global behavior parity).

## 2026-03-11T18:21:00-03:00

### Follow-up refinement
- Added explicit sidebar hover guard so auto-hide cannot collapse while the cursor is inside the sidebar.
- `Sidebar.svelte` now exposes optional `onMouseEnter` / `onMouseLeave` callbacks.
- `+page.svelte` now tracks `sidebarHovered`; auto-hide scheduling is skipped while hovered, and timer is cleared on enter.

### Validation delta
- Behavior: sidebar remains open while hovered, and resumes 3s auto-hide countdown after leave/activity.
