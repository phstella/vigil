# 3.9-fix: Register `get_note_graph` backend command

## 2026-03-12T23:30 — Implementation

**Problem:** The frontend `getNoteGraph()` IPC call invokes `get_note_graph`, but no
Tauri command was defined or registered, causing the graph view to always fall back
to mock demo data.

**Changes made:**

1. **`src-tauri/src/commands/links.rs`** — Added `get_note_graph` async Tauri command
   that checks workspace/index availability, then delegates to
   `LinkGraph::get_graph(&index)`. Updated import to include `NoteGraphResponse`.

2. **`src-tauri/src/lib.rs`** — Registered `commands::links::get_note_graph` in the
   `generate_handler![]` macro, after `get_backlinks`.

3. **`src/lib/features/graph/graph-store.ts`** — Updated catch-block comment from
   "deferred to Epic 4" to generic fallback message. Updated `console.warn` text.

**Validation results:**

| Command | Result |
|---------|--------|
| `npm run check` | PASS (0 errors, 2 pre-existing a11y warnings) |
| `npm run lint` | PARTIAL — 1 pre-existing error in NoteEditor.svelte (not our file) |
| `npm run build` | PASS |
| `npm run format:check` | PARTIAL — 26 pre-existing format issues (not our changes) |
| `cargo fmt --check` | SKIPPED — cargo not available on this machine |
| `cargo clippy` | SKIPPED — cargo not available on this machine |
| `cargo test` | SKIPPED — cargo not available on this machine |

**Risks:**
- Rust compilation not verified locally (no cargo toolchain). CI must confirm the
  Tauri command compiles and `get_graph` signature matches expectations.
