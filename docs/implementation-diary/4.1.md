# Implementation Diary: Task 4.1 - Extend Omnibar Command Mode

## 2026-06-02 - Implementation

**Goal:** Add internal command actions to the omnibar while preserving file and content search modes.

**Changes made:**
- Added `src/lib/features/omnibar/omnibar-parser.ts` to parse `>` command queries and normalize command-mode input.
- Extended `OmnibarMode` with `command`.
- Extended `omnibar-store.svelte.ts` with local command definitions, command result matching, and command-mode selection.
- Updated `Omnibar.svelte` with a Commands tab, `Ctrl+Shift+P` command mode, and command execution on Enter/click.
- Updated `OmnibarItem.svelte` to render command rows alongside file/content rows.
- Registered app-level commands in `src/routes/+page.svelte`:
  - Save current file
  - New note
  - Toggle sidebar
  - Show explorer/search/graph
  - Toggle side-by-side editor
  - Toggle markdown preview
  - Close active tab

**Validation:**
- `npm run check`: PASS (0 errors, 0 warnings)
- `npm run lint`: PASS
- `npm run build`: PASS (existing Vite chunk-size/static+dynamic import warnings only)
- `npm run format:check`: PASS
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all --check`: PASS
- `cargo clippy --manifest-path src-tauri/Cargo.toml --workspace --all-targets -- -D warnings`: PASS
- `cargo test --manifest-path src-tauri/Cargo.toml --workspace`: PASS

**Acceptance mapping:**
- File queries still use existing fuzzy search mode -- PASS
- Content queries still use existing content search mode -- PASS
- `> command` and `Ctrl+Shift+P` expose internal actions in the same omnibar -- PASS
- Command execution works through local app shell actions, with no marketplace/plugin-store flow -- PASS

**Open items:**
- Manual desktop smoke should verify `Ctrl+Shift+P`, `>` prefix, Enter execution, and click execution in Tauri runtime.
