# Rust formatting configuration for Vigil
# Requires: rustfmt (included with rustc via system package or rustup)
edition = "2021"
max_width = 100
tab_spaces = 4
use_field_init_shorthand = true
use_try_shorthand = true
